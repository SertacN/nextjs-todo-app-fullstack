import ThemeButton from "./ThemeButton";

export const Navbar = () => {
  return (
    <header className="py-1 mb-5 flex items-center justify-center w-auto">
      <ThemeButton />
    </header>
  );
};
